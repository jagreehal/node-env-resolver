---
'node-env-resolver-aws': minor
---

Add CommonJS (CJS) support alongside ESM. The package now builds both formats, allowing it to be used in both ESM and CommonJS environments. Updated build configuration to match the main `node-env-resolver` package.
